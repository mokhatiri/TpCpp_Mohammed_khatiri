# To run:
run the commmand make in the terminal
```sh
make
```

then run the executable
supposing the exercice of interest is exercice 'k'
```sh
./exok.exe
```

# To clean:
to clean the directory
```sh
make clean
```

# the exerices:
1. exo1:
    - // Detection de palindrome (string , reverse)
2. exo2
    - // Frequence des mots dans un texte (std::unordered_map, std::istringstream)
3. exo3
    - // Compression de texte (encodage RLE)
4. exo4
    - // extraction des mots les plus frequents d'un texte (std::map)
5. exo5
    - // Trier les mots par longueur (std::multimap)
6. exo6
    - // advanced search using regex (std::regex)